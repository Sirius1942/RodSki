## 问题1：
| **navigate** | 导航到 URL | — | URL 或 GlobalValue 引用 保留，去掉open，如果当前没有浏览器对象则打开新增。如果有则复用对象，导航到相关页面。注意修改后，要修改所有的示例用例和相关文档说明。不需要兼容老用例，废弃open

## 问题2：
下面我们改另外一个主要问题，测试用例中没有ui自动化的原子动作关键字，比如 hover等 这些都没有，这些关键字都是要在数据表的对应元素的表格中写上关键字。这里特殊的是键盘输入，key_press 数据表中输入格式是 key_press【按键】，修改代码实现逻辑。和用户说明书相关内容。注意用户说明书中 key_press 一些特殊按键的用法比如control+c 要特别说明。

## 问题3：
| **run** | 运行 Logic 子用例 | — | 子用例名称 | 这里的作用改一下，去掉logic的功能，run以后是特定指在沙箱环境下执行特定的代码，代码片段以工程形式保存在和case文件夹同级别目录，文件夹名fun，下面是具体工程名（既模型名），数据列是执行的具体代码路径。目前只支持python代码 代码的返回值自动保存到测试步骤执行结果中。可以在其它步骤数据表中通过return关键字获取。

## 问题4：
1、没有这个元素 verify替代 httpget post put delete send 没有这4个关键字，send 替代用例中不出现这5个关键字代码也不支持。同时，接口自动化测试的接口模型中要有元素设定界面的请求模式。assent 没有这两个断言 这两个断言都是 | `verify` | 批量验证（模型 + 数据） |模型一部分，接口自动化测试verify的返回值数据表中默认要有 status列 和具体的字段期望值。这样让verify就可以完成接口自动化测试的结果断言判断。

## 问题5：
2、新增一个需求，数据表中加一个类型 模型名_verify 代表模型的批量校验数据。测试用例里面 通过verify调用这个数据表中的数据。

## 问题6：
3、数据列 说明这里有一个错误的地方，这里不需要写sheet的表名 直接写DataID,因为模型名与数据表表明要求 强制一致。 verify时，自动匹配带verify的数据表。