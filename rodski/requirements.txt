openpyxl>=3.1.0
xmlschema>=2.5.0
playwright>=1.40.0
# 注意: RodSki 不依赖 pytest 等外部测试框架
# 自检使用自有执行器: python selftest.py
Appium-Python-Client>=3.0.0
PyQt6>=6.6.0
tqdm>=4.65.0
psutil>=5.9.0

# 视觉定位模块依赖
requests>=2.31.0
anthropic>=0.18.0
openai>=1.12.0
pillow>=10.0.0
pyyaml>=6.0

# 数据库驱动 (按需安装)
# pymysql>=1.1.0       # MySQL / MariaDB
# psycopg2>=2.9.0      # PostgreSQL
# pymssql>=2.2.0       # SQL Server

# PDF 导出支持 (可选)
# pdfkit>=1.0.0  # 需要 wkhtmltopdf
# weasyprint>=60.0  # 纯 Python 实现
