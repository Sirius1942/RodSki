# RodSki 单元测试执行报告

> 日期: 2026-04-13 08:04:20 +0800
> 分支: main
> Commit: `ef43fc0`
> pytest 版本: 8.4.2
> 范围: `tests/unit`

---

## 一、执行命令

```bash
python3 -m pytest tests/unit -q
```

---

## 二、执行结果汇总

| 指标 | 结果 |
|------|------|
| passed | **1049** |
| failed | **0** |
| xfailed | **3** |
| skipped | **0** |
| warnings | **9** |
| 总耗时 | **37.59s** |

```text
1049 passed, 3 xfailed, 9 warnings in 37.59s
```

---

## 三、关键说明

### 1. 当前套件状态

- 单元测试全量执行通过，无失败用例
- 存在 3 个 `xfail`，用于显式记录 `core/dynamic_executor.py` 中已知源码问题
- 当前结果满足“代码修改后必须通过单元测试”的交付门禁

### 2. xfail 背景

已知 `xfail` 主要对应以下问题：

1. `>=` / `<=` 运算符解析顺序问题
2. `text_not_contains(...)` 正则匹配问题

这些问题已通过测试用例显式标注，未被静默忽略。

---

## 四、Warnings 摘要

1. `urllib3` / LibreSSL 环境告警：当前 Python `ssl` 模块使用 `LibreSSL 2.8.3`
2. `PytestCollectionWarning`：`core/test_case_explainer.py` 中 `TestCaseExplainer` 类带 `__init__`，pytest 不收集该类为测试类
3. `DeprecationWarning`：`keyword_engine.py` 中 `get_text` 已废弃，建议后续迁移到 `get`

---

## 五、可交付结论

- **结论**: 可交付
- **原因**: 全量单元测试通过（1049 passed, 0 failed），仅保留已明确标注原因的 `xfail`
- **报告归档位置**: `.pb/iterations/iteration-25/test_report_2026-04-13_0804.md`
