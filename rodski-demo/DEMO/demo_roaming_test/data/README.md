# Runtime data

`run_demo.py` creates `data.sqlite` with a `RoamForm` logical table containing
base row `D001` and roaming candidate `D002`. The database is generated with
the Python standard library and removed after acceptance.
