<?xml version="1.0" encoding="UTF-8"?>
<datatables>
    <!-- 登录验证 -->
    <datatable name="Login_verify">
        <row id="V001">
            <field name="loginMessage">登录成功</field>
        </row>
        <row id="V002">
            <field name="loginMessage">登录失败</field>
        </row>
    </datatable>

    <datatable name="ErrorMessage_verify">
        <row id="V001">
            <field name="errorMsg">用户名或密码错误</field>
        </row>
    </datatable>

    <!-- API验证 -->
    <datatable name="LoginAPI_verify">
        <row id="V001">
            <field name="success">true</field>
            <field name="token">demo_token_123</field>
        </row>
    </datatable>

    <datatable name="OrderAPI_verify">
        <row id="V001">
            <field name="status">success</field>
        </row>
    </datatable>

    <!-- 表单验证 -->
    <datatable name="Form_verify">
        <row id="V001">
            <field name="formResult">提交成功: 张三</field>
        </row>
    </datatable>

    <datatable name="TestForm_verify">
        <row id="V001">
            <field name="result">测试成功</field>
        </row>
    </datatable>

    <!-- 看板验证 -->
    <datatable name="Dashboard_verify">
        <row id="V001">
            <field name="totalOrders">3</field>
            <field name="completedOrders">2</field>
        </row>
    </datatable>

    <!-- 订单验证 -->
    <datatable name="Order_verify">
        <row id="V001">
            <field name="orderStatus">已创建</field>
        </row>
    </datatable>

    <datatable name="OrderTable_verify">
        <row id="V001">
            <field name="orderCount">3</field>
        </row>
    </datatable>

    <!-- 产品验证 -->
    <datatable name="Product_verify">
        <row id="V001">
            <field name="productName">iPhone 15</field>
        </row>
    </datatable>

    <!-- 用户验证 -->
    <datatable name="User_verify">
        <row id="V001">
            <field name="username">testuser</field>
        </row>
    </datatable>

    <!-- 数据库查询验证 -->
    <datatable name="QuerySQL_verify">
        <row id="V001" remark="验证查询结果不为空">
            <!-- [空校验] order_no 取决于初始 DB 数据，暂无稳定字面值；待固定种子数据后修正 -->
            <field name="order_no">${Return[-1][0].order_no}</field>
        </row>
        <row id="V002" remark="验证插入的订单">
            <field name="order_no">TEST001</field>
            <field name="customer_name">测试用户</field>
            <field name="total_amount">999.00</field>
        </row>
        <row id="V003" remark="验证统计结果">
            <!-- [空校验] COUNT(*) 结果取决于 DB 当前行数，暂无稳定字面值；待固定种子数据后修正 -->
            <field name="total">${Return[-1][0].total}</field>
        </row>
    </datatable>

    <datatable name="QueryMySQL_verify">
        <row id="V001" remark="验证查询结果不为空">
            <!-- [空校验] order_no 取决于初始 DB 数据，暂无稳定字面值；待固定种子数据后修正 -->
            <field name="order_no">${Return[-1][0].order_no}</field>
        </row>
        <row id="V002" remark="验证插入的订单">
            <field name="order_no">MYSQL_TEST001</field>
            <field name="customer_name">MySQL测试用户</field>
            <field name="total_amount">1999.00</field>
        </row>
    </datatable>

    <datatable name="QueryDB_verify">
        <row id="V001">
            <field name="count">5</field>
        </row>
    </datatable>

    <datatable name="QueryOrder_verify">
        <row id="V001">
            <field name="status">completed</field>
        </row>
    </datatable>

    <datatable name="QueryUser_verify">
        <row id="V001">
            <field name="username">admin</field>
        </row>
    </datatable>

    <!-- Return测试验证 -->
    <datatable name="ReturnTest_verify">
        <row id="V001">
            <field name="result">${Return[-1]}</field>
        </row>
    </datatable>

    <!-- UI动作验证 -->
    <datatable name="UIActions_verify">
        <row id="V001">
            <field name="result">操作成功</field>
        </row>
    </datatable>

    <!-- set/get 验证数据 -->
    <datatable name="SetGetVerify_verify">
        <row id="V001" remark="验证get读取first_result等于T001提交值">
            <field name="value">${Return[-1]}</field>
        </row>
    </datatable>

    <!-- get选择器模式验证 -->
    <datatable name="GetVerify_verify">
        <row id="V001" remark="验证get选择器模式结果写入history">
            <field name="result">${Return[-1]}</field>
        </row>
    </datatable>

    <!-- evaluate结构化返回验证 -->
    <datatable name="EvaluateResult_verify">
        <row id="V001" remark="验证evaluate返回title字段">
            <field name="title">${Return[-1].title}</field>
        </row>
    </datatable>

    <!-- get模型模式验证 -->
    <datatable name="GetModelVerify_verify">
        <row id="V001" remark="验证get模型模式返回dict">
            <field name="formResult">${Return[-1].formResult}</field>
        </row>
    </datatable>

    <!-- DemoForm Auto Capture验证 -->
    <datatable name="DemoFormVerify_verify">
        <row id="V001" remark="验证auto_capture提取resultId">
            <field name="resultId">${Return[-1].resultId}</field>
        </row>
    </datatable>

    <!-- LoginAPICapture验证 -->
    <datatable name="LoginAPICapture_verify">
        <row id="V001" remark="验证send auto_capture提取token">
            <field name="data.token">${Return[-1]._capture.token}</field>
        </row>
    </datatable>

    <!-- 定位器测试验证 -->
    <datatable name="LocatorTest_ID_verify">
        <row id="V001" remark="验证ID定位器结果">
            <field name="result">测试结果: ID 定位器测试成功！</field>
        </row>
    </datatable>

    <datatable name="LocatorTest_Name_verify">
        <row id="V001" remark="验证Name定位器结果">
            <field name="result">测试结果: Name 定位器测试成功！</field>
        </row>
    </datatable>

    <datatable name="LocatorTest_CSS_verify">
        <row id="V001" remark="验证CSS定位器结果">
            <field name="result">测试结果: CSS 定位器测试成功！</field>
        </row>
    </datatable>

    <datatable name="LocatorTest_XPath_verify">
        <row id="V001" remark="验证XPath定位器结果">
            <field name="result">测试结果: XPath 定位器测试成功！</field>
        </row>
    </datatable>

    <datatable name="LocatorTest_ScrollClick_verify">
        <row id="V001" remark="验证视口外按钮点击结果">
            <field name="result">测试结果: ScrollClick 定位器测试成功！</field>
        </row>
    </datatable>

    <!-- 关键字测试验证 -->
    <datatable name="KeywordTest_verify">
        <row id="V001" remark="验证提交结果">
            <field name="formResult">提交成功: 关键字测试内容</field>
        </row>
        <row id="V002" remark="验证get获取的文本">
            <field name="formResult">${Return[-1]}</field>
        </row>
    </datatable>

    <!-- 视觉定位验证 -->
    <datatable name="VisionCoordinate_verify">
        <row id="V001" remark="验证坐标定位结果">
            <field name="formResult">提交成功: 坐标定位测试</field>
        </row>
    </datatable>

    <!-- 多窗口验证 -->
    <datatable name="NewWindow_verify">
        <row id="V001" remark="验证新窗口内容">
            <field name="newWindowTitle">新窗口标题</field>
            <field name="newWindowContent">这是新窗口的内容</field>
        </row>
    </datatable>

    <!-- iframe验证 -->
    <datatable name="IframeContent_verify">
        <row id="V001" remark="验证iframe内容">
            <field name="iframeTitle">iframe标题</field>
            <field name="iframeText">这是iframe中的内容</field>
        </row>
    </datatable>

    <!-- 负面测试验证 -->
    <datatable name="ErrorAPI_verify">
        <row id="E001" remark="期望的错误响应（实际不会匹配）">
            <field name="detail">Expected error message that will not match</field>
        </row>
    </datatable>

</datatables>
