<?xml version="1.0" encoding="UTF-8"?>
<!--
  Android 多设备队列验收 —— 短计划 A（登录失败提示，3 步）。

  与 mixed_* 系列（iOS 真机 + iOS 模拟器）对应：这里是 **Android 真机 +
  Android 模拟器** 的异构混跑。两台设备平台相同、设备类别不同 —— 真机走 USB
  上的实体手机，模拟器走 AVD，两者在 Appium/UiAutomator2 内部是不同的会话路径。

  长度刻意不等（三个计划才能观察到动态领取）：
    android_queue_a（APP002，最短）→ 先跑完的设备**必须**回头再领一个
    android_queue_b（APP003，最长）→ 独占一台设备，且全过程不被拆分
    android_queue_c（APP001，最短）→ 「第二班」计划，证明动态领取

  设备数量与组合由 data/globalvalue.xml 的 Mobile 组决定
  （DeviceCount=2 / DeviceMix=real,simulator），**不在命令行写 UDID**。

  前置：mock_server 已启动 + adb reverse tcp:8000 tcp:8000 + Appium server
  + 真机与模拟器都已安装 com.rodski.demo。详见 README.md「Android 混跑验收」。
  执行入口：scripts/run_android_mixed_demo.sh
-->
<test_plan id="android_queue_a"
           title="队列 demo（Android）—— 短计划：登录失败"
           kind="suite"
           execute="是"
           default_execute="否">
  <case id="APP002" execute="是"/>
</test_plan>
