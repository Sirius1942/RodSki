<?xml version="1.0" encoding="UTF-8"?>
<!--
  多设备计划队列 demo —— 短计划 C（登录成功，3 步）。

  「第二班」计划：它排在队列最末，只有当先跑完短计划 A 的那台设备**回来再领一个**
  时才会被执行。若调度器是静态分配（每台设备固定领 N/设备数 个），
  本计划会落在长计划 B 的设备上或根本不被执行——两种都能被 demo 断言捕获。
  详见 queue_short_a.xml 的排班图与 README.md「双设备并行验收」。
-->
<test_plan id="queue_short_c"
           title="队列 demo —— 短计划 C（登录成功）"
           kind="suite"
           execute="是"
           default_execute="否">
  <case id="APP001" execute="是"/>
</test_plan>
